# Tools: Carlos Bennett

## Tool Usage

### Connected Services

#### Communications & Inbox

- **Gmail** (`gmail-api`): Primary thread for Claire Walsh's bookings, guitar repair clients, Coast Guard, Gulf Coast Marine Insurance, and Rick Morgan. Account `carlos.bennett@Finthesiss.ai`.
- **Outlook** (`outlook-api`): Stands by for any corporate charter coordinator or supplier who works out of an Outlook shop. Carlos lives on the Gmail side.
- **SendGrid** (`sendgrid-api`): Configured for batched repair-pickup notifications when the workshop pipeline gets thick.
- **Mailgun** (`mailgun-api`): Could handle transactional sends for the guitar shop, but volume sits below threshold so it stays quiet here.
- **Twilio** (`twilio-api`): SMS to Russ on engine alarms, Nolan on weather reads, and last-minute charter confirmations when email is too slow.
- **WhatsApp** (`whatsapp-api`): On hand for the rare out-of-state collector or vintage-parts contact who lives on WhatsApp.
- **Slack** (`slack-api`): Stands by in case the harbor association adopts a workspace. The dock still runs on phone calls.
- **Microsoft Teams** (`microsoft-teams-api`): Configured for the occasional corporate charter group that operates in Teams.
- **Telegram** (`telegram-api`): Ready for the vintage parts hunt when a seller insists on it.
- **Discord** (`discord-api`): Read-only on the luthier forum servers where Carlos lurks for fret-leveling and bridge-plate threads.
- **Zoom** (`zoom-api`): Set up for the occasional remote consult with a guitar client who is shipping an instrument in for restoration sight-unseen.

#### Calendar, Scheduling, Forms

- **Google Calendar** (`google-calendar-api`): The four-spine calendar lives here. Charter season block (May 1 to Nov 30 default), repair deadlines, family weeks, Coast Guard milestones.
- **Calendly** (`calendly-api`): On standby if Claire ever opens a self-serve booking window for off-peak charter dates.
- **Typeform** (`typeform-api`): Configured for charter intake forms when a corporate group wants a roster on file before the trip.

#### Files, Documents, Notes

- **Google Drive** (`google-drive-api`): Boat maintenance log spreadsheet, guitar repair invoice files, Coast Guard packet, charter booking records, seasonal revenue tracker.
- **Dropbox** (`dropbox-api`): Ready for the occasional client who sends high-resolution photos of an instrument before drop-off.
- **Box** (`box-api`): Stands by for any corporate charter client who lives in Box for waivers and rosters.
- **Notion** (`notion-api`): Could carry the repair pipeline, but Sheets is where Carlos keeps it.
- **Obsidian** (`obsidian-api`): On hand if Carlos ever decides to keep the repair notes locally and offline.
- **Confluence** (`confluence-api`): Stays quiet here. No team wiki to feed.
- **Airtable** (`airtable-api`): Could track the parts inventory more cleanly than Sheets, but the cost of switching is not worth the gain.

#### Charter Operations, Weather, Maps

- **OpenWeather** (`openweather-api`): Bayport coastal forecast, tide-window cross-checks, and front-edge thunderstorm reads for the 3:30 AM briefing.
- **Google Maps** (`google-maps-api`): Truck routing to Brooksville, Spring Hill, Tampa, and the parts run loop, plus dock and ramp lookups for visiting captains.
- **NASA** (`nasa-api`): Satellite imagery for Gulf surface conditions and bait-tide context when the local feeds disagree.

#### Guitar Parts, Marketplaces, Shipping

- **Amazon Seller** (`amazon-seller-api`): Read-only for parts pricing and the occasional fast-ship string set. Carlos does not list inventory there.
- **Etsy** (`etsy-api`): On hand for the rare hand-wound pickup or specialty inlay piece a vintage build needs.
- **BigCommerce** (`bigcommerce-api`): Stands by if Bennett Guitar Works ever opens an online storefront. No plan to.
- **WooCommerce** (`woocommerce-api`): Configured for the same hypothetical storefront, same answer.
- **Shippo** (`shippo-api`): Sorts the carrier rate when a restored instrument needs to ship to an out-of-state client.
- **FedEx** (`fedex-api`): Standard carrier for finished instruments going to Tampa, Atlanta, and points north.
- **UPS** (`ups-api`): Backup carrier and the default for incoming parts from StewMac and LMI.

#### Marketing & Web Presence

- **Instagram** (`instagram-api`): Read-only. Diane occasionally cross-posts repair-bench shots from the Facebook page; Carlos does not run the account.
- **Pinterest** (`pinterest-api`): On standby. Stays quiet here.
- **Mailchimp** (`mailchimp-api`): Could run a charter newsletter list, but Claire's pipeline keeps the calendar full without one.
- **Klaviyo** (`klaviyo-api`): Configured for the hypothetical newsletter. Same answer as Mailchimp.
- **ActiveCampaign** (`activecampaign-api`): Stays quiet here. Same shape of unused capacity.
- **WordPress** (`wordpress-api`): On hand if Diane ever decides Bennett Guitar Works needs a proper site beyond the Facebook page.
- **Webflow** (`webflow-api`): Configured for the same possibility, designer-friendly side.
- **Contentful** (`contentful-api`): Stands by for any future site that grows past a flat page.

#### Finance, Payments, Investing

- **QuickBooks** (`quickbooks-api`): Charter ledger and the guitar shop invoicing live here, separated by class so tax time is honest.
- **Xero** (`xero-api`): On standby in case Rick Morgan ever moves Carlos onto it.
- **Stripe** (`stripe-api`): Configured for online repair deposits. Most clients still pay on pickup with check or cash.
- **Square** (`square-api`): The dock reader Russ knows how to run when a charter tips on card.
- **PayPal** (`paypal-api`): Out-of-state repair clients use this when they pay ahead. Carlos prefers checks.
- **Plaid** (`plaid-api`): Connects the Coastal Bank checking and savings views into the bookkeeping side.
- **DocuSign** (`docusign-api`): Charter liability waivers, vendor invoices over $1,000, and the occasional Coast Guard packet attachment.
- **Alpaca** (`alpaca-api`): Read-only mirror of the IRA position Rick Morgan manages at Gulf Financial Partners.
- **Coinbase** (`coinbase-api`): Stays quiet here. Carlos is not in crypto.
- **Binance** (`binance-api`): Same posture. Read context only.
- **Kraken** (`kraken-api`): Same. Stays quiet here.

#### Music, Entertainment, Travel

- **Spotify** (`spotify-api`): B.B. King, Stevie Ray Vaughan, Muddy Waters, CCR, Allman Brothers, Hank Williams, George Jones. The workshop playlist runs through this.
- **YouTube** (`youtube-api`): Fret-leveling reference clips, Cummins diesel diagnostic videos, and the occasional Harbor Ramblers practice upload.
- **Vimeo** (`vimeo-api`): Stands by for any restoration walkthrough a client wants in long-form.
- **TMDB** (`tmdb-api`): On hand for old Westerns and the fishing-show guilty pleasure when the evening calls for it.
- **Ticketmaster** (`ticketmaster-api`): College football season tickets with Diane and the rare blues show within drive distance.
- **Eventbrite** (`eventbrite-api`): Tracks the Bayport waterfront events Carlos sits in on with the Harbor Ramblers.
- **Twitch** (`twitch-api`): Stays quiet here.
- **OpenLibrary** (`openlibrary-api`): The paperback he packs and probably will not finish on any trip Diane talks him into.
- **Amadeus** (`amadeus-api`): Configured for any long-haul trip beyond drive distance. Carlos travels rarely.
- **Airbnb** (`airbnb-api`): On hand for the long-weekend-with-Diane radius around the Gulf Coast.
- **Uber** (`uber-api`): Tampa airport runs and the occasional ride home after a tailgate.
- **DoorDash** (`doordash-api`): Stands by when the kitchen is not the answer. Carlos usually cooks.
- **Instacart** (`instacart-api`): Configured for the rare week the dock and the workshop both run hot and the grocery run loses.

#### Family, Home, and Neighborhood

- **Zillow** (`zillow-api`): Local market context when Marcel or Luc ask whether a friend's listing is fair. The cottage is not on the market.
- **Ring** (`ring-api`): Front-porch and dock-side cameras Diane talked Carlos into during a parts-theft scare last year.
- **Yelp** (`yelp-api`): Sanity-check reviews on a new restaurant before he and Diane try it, plus reads on out-of-town stops.
- **Reddit** (`reddit-api`): Read-only on the luthier and Cummins diesel subs where a niche fix sometimes lives.
- **Twitter** (`twitter-api`): Stays quiet here. Carlos does not post.
- **LinkedIn** (`linkedin-api`): Read-only context on a corporate charter contact Claire forwards.
- **Google Classroom** (`google-classroom-api`): On hand for Elise's school updates when Luc or Ashley forwards a permission slip Carlos is on for pickup.

#### Health & Body

- **MyFitnessPal** (`myfitnesspal-api`): Could log the cholesterol-side cuts Dr. Mitchell asked about. Carlos has not opened it.
- **Strava** (`strava-api`): Stays quiet here. The boat and the property are the workout.

#### CRM, Support, and Business Operations

- **HubSpot** (`hubspot-api`): Could carry the repair client list, but Carlos keeps it in Sheets and his head.
- **Salesforce** (`salesforce-api`): Stands by for the occasional corporate charter coordinator who lives in it.
- **Intercom** (`intercom-api`): Stays quiet here. No customer chat surface to feed.
- **Freshdesk** (`freshdesk-api`): Same posture. On standby only.
- **Zendesk** (`zendesk-api`): Same. Stays quiet here.
- **ServiceNow** (`servicenow-api`): Could handle a corporate-side ticket if a fleet manager ever insists on one. No active queue.
- **BambooHR** (`bamboohr-api`): On hand if Bennett Guitar Works ever runs more than Russ on the payroll. It will not.
- **Greenhouse** (`greenhouse-api`): Stays quiet here. No hiring pipeline.
- **Gusto** (`gusto-api`): Configured for Russ's seasonal pay if Carlos ever moves him off the weekly check.

#### Developer, DevOps, and Analytics Stack

- **GitHub** (`github-api`): Read-only. Watches Marcel's recipe-side repo and a couple of luthier forks for fret-calculator scripts.
- **GitLab** (`gitlab-api`): Stays quiet here. No mirror to follow.
- **Linear** (`linear-api`): On standby. Sheets carries the repair pipeline.
- **Jira** (`jira-api`): Stands by for the corporate charter that runs trips off a Jira project. Rare.
- **Trello** (`trello-api`): Could carry the repair pipeline in cards. Carlos prefers the spreadsheet.
- **Asana** (`asana-api`): Same posture. Stays quiet here.
- **Monday** (`monday-api`): Same.
- **Figma** (`figma-api`): On hand for the rare bridge-plate or pickguard template Diane wants to mock up before he routes it.
- **Sentry** (`sentry-api`): Stays quiet here. No app to instrument.
- **Datadog** (`datadog-api`): Same.
- **PagerDuty** (`pagerduty-api`): Could route engine-alarm escalations to Russ, but the VHF and the phone tree handle it.
- **Okta** (`okta-api`): Stands by for the corporate charter sign-in if it ever comes up.
- **Cloudflare** (`cloudflare-api`): Configured for the hypothetical Bennett Guitar Works site that does not exist yet.
- **Kubernetes** (`kubernetes-api`): Stays quiet here. Nothing to orchestrate.
- **Google Analytics** (`google-analytics-api`): Read-only on the Facebook page insights Diane shares when she pulls them.
- **Mixpanel** (`mixpanel-api`): Stays quiet here.
- **Amplitude** (`amplitude-api`): Same.
- **PostHog** (`posthog-api`): Same.
- **Segment** (`segment-api`): Same.
- **Algolia** (`algolia-api`): On standby for the Bennett Guitar Works site that might exist someday.

#### Not Connected

- Live web search, web browsing, and deep internet research are not available. The agent works only from connected mock APIs and stored memory.
- The Bennett Guitar Works Facebook page is managed by Diane Crawford; the agent does not post, comment, or message through it.
- The marine GPS chartplotter and VHF radio on the Lucky Strike are standalone on-boat hardware. Carlos operates them directly.
- The NOAA Weather radio and the iPhone NOAA app are not API-connected; Carlos checks them in hand.
- The Walsh Charter Services internal booking system is Claire Walsh's source of truth; the agent cross-references against the Gmail thread only.
- The USCG renewal portal at the National Maritime Center is submitted by Carlos directly; the agent drafts the packet checklist.
- Coastal Bank's online banking interface is not connected directly. The Plaid feed gives the read-only view only.
- Diane Crawford's personal accounts and Coastal Family Dentistry's systems are not connected and not to be touched.
